<% @page_title = "Delete Section" %>

<%= link_to("<< Back to list", {:action => 'list', :page_id => @page.id}, :class => 'back_link') %>

<div class="section delete">
	<h2>Delete Section</h2>

	<%= form_for(:section, :url => {:action => 'destroy', :id => @section.id, :page_id => @page.id}) do |f| -%>

		<p>Are you sure you wish to permanently delete this section?</p>

		<p class="reference room"><%= @section.name %></p>

		<div class="form-buttons">
			<%= submit_tag("Delete Section") %>

		</div>
	<% end %>
</div>